my current FE : https://cv-lens-prod.netlify.app
my current BE : https://grow-19-cvlens-1.onrender.com

The Flow which is working : 
1. FE - User Registration @RegistrationView
2. FE - useRegisterMutation
2. BE - authRoutes.route("/register").post(NoAuth, registerUser);
3. BE - registerUser 
      - req.login(newUser)
      - serializeUser(user, done)
      - done(null, user._id.toString())
      - req.session = { passport : { user: user.id } }
      - req.session.save()
      - - Cookie behavior:
        - save into redisStore
        - sends cookie

4. FE - useRegisterMutation - onSuccess 
      - invalidateQueries["user"]
      - useUserQuery refetches the user
      - AuthProvider - query.isSuccess
      - PublicOnlyRoute - redirect to "/app"
      - Works perfectly user logged in successFully


Now the Broken Part - 
1. User Logging out.
      - FE NavBar - handleLogut
      - BE await destroySessionAndLogout(req, res);
      - BE session get destroyed from redis ( if I am right )

2. Now user trying to do "Continue with Google - with any other Emial ID"
3. FE - RegisterView or LoginView 
      - <GoogleBtn href={`${BE_ORIGIN}/api/auth/google`} />
4. Google page asking which Email Id to use user choose an gamilid <aj19990321@gmail.com>
   BE - authe.route.ts 
      - route("/google")
      - route("/google/callback")
      - passport.config.ts - ensureUserService
      - createUserService
      - user with gmail <aj19990321@gmail.com> get created on neon db ( checked already )
      - a new sessionId stored in redis ( g19-session:WtUYoQ-MmY0uFwM9y1GFp5DoJkEpD8O- ) ( checked already )
      - handleGoogleAuthSuccess
      - ${config.FRONTEND_ORIGIN}/app
   FE - ProtectedRoute 
      - useAuthContext()
      - useUserQuery
      - getUser
      - console.log()
      - console.log(errorData);
      - Object { success: false, status: "error", message: "Authentication required", env: "production", errorCode: "AUTH_UNAUTHORIZED_ACCESS" }
      - <Navigate to="/login" replace /> ( Protected-Route as !isAuthenticated == true )
      - toast("User not logged in !", { id: TOAST_ID, icon: "⚠️" })

Well then say user tries again "Continue with Google"
- BE - NoAuth middleware
     - Error - "Authenticated user can't perform this actions"
     - but in FE user not logged in ?? This is the Broken Part
